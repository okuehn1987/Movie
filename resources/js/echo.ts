// import Echo from 'laravel-echo';

// import Pusher from 'pusher-js';

// declare global {
//     interface Window {
//         Echo: Echo;
//         Pusher: typeof Pusher;
//     }
// }

// window.Pusher = Pusher;

// window.Echo = new Echo({
//     broadcaster: 'reverb',
//     key: 'livepost_key',
//     wsHost: location.hostname,
//     wsPort: import.meta.env.DEV ? 6001 : 80,
//     wssPort: import.meta.env.DEV ? 6001 : 443,
//     forceTLS: import.meta.env.DEV ? false : 'https',
//     encrypted: false,
//     enabledTransports: ['ws', 'wss'],
// });
